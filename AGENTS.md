# 저장소 기여 가이드

## 프로젝트 구조 및 모듈 구성

이 저장소는 Godot 4.7 기반의 GDScript 프로젝트입니다. `project.godot`이 애플리케이션을 설정하고 `main.tscn`을 시작 장면으로 실행합니다. 런타임 코드는 `scripts/`에 있습니다. `main.gd`는 화면 그리기와 입력, `board_state.gd`는 게임 상태 전환, `path_rule.gd`는 화살표 경로 판정, `arrow_placement.gd`는 해결 가능한 무작위 배치, `stage_catalog.gd`는 스테이지 데이터와 검증을 담당합니다. 헤드리스 회귀 테스트는 `tests/`에 있습니다. `.godot/`은 에디터가 생성하는 파일이므로 직접 수정하거나 커밋하지 마세요.

## 빌드, 테스트 및 개발 명령

모든 명령은 저장소 루트에서 실행합니다. 아래 예시는 `godot` 명령이 Godot 4.7.1 실행 파일을 가리킨다고 가정합니다.

```bash
godot --editor --path .                         # 프로젝트를 에디터에서 열기
godot --path .                                  # 메인 장면 실행
godot --headless --log-file /tmp/pickup-arrow.log --path . \
  --script tests/test_gameplay_core.gd           # 단일 테스트 스위트 실행
```

게임플레이 변경을 제출하기 전에 `test_gameplay_core.gd`, `test_stage_catalog.gd`, `test_playable_flow.gd`, `test_arrow_placement.gd`를 모두 실행하세요. Godot 프로젝트에는 별도의 컴파일 단계가 필요하지 않습니다.

## 코딩 스타일 및 이름 규칙

기존 GDScript 스타일을 따릅니다. 들여쓰기에는 탭을 사용하고, 변수와 함수는 `snake_case`, `class_name` 선언은 `PascalCase`, 상수는 `UPPER_SNAKE_CASE`로 작성하세요. 타입을 알 수 있는 매개변수, 반환값, 지역 변수에는 정적 타입을 지정합니다. 상태 변경은 `BoardState`, 렌더링과 입력은 `PickupArrowGame`에 배치하세요. 프로젝트 리소스에는 `res://` 경로를 사용하고, 스테이지 ID는 `STAGE-004`처럼 안정적인 형식을 유지합니다.

## 테스트 지침

테스트는 외부 프레임워크 없이 실행되는 독립형 `SceneTree` 스크립트입니다. `_test_*` 메서드와 `_expect()` 단언을 사용하며, 새 파일은 `tests/test_<기능>.gd` 형식으로 이름을 정하세요. 규칙 변경, 상태 전환, 잘못된 스테이지 데이터, 실제 입력 흐름에는 회귀 테스트를 추가해야 합니다. 모든 테스트는 Godot 오류 없이 종료 코드 `0`을 반환해야 합니다.

## 커밋 및 풀 리퀘스트 지침

현재 체크아웃에서는 Git 기록을 확인할 수 없으므로, `막힌 화살표 회귀 테스트 추가`처럼 짧고 명령형인 커밋 제목을 사용하세요. 커밋 하나에는 한 가지 목적만 담습니다. 풀 리퀘스트에는 플레이어가 체감하는 변경 사항, 실행한 테스트, 관련 이슈를 기재하세요. 화면이나 상호작용이 바뀌면 스크린샷 또는 짧은 영상을 첨부합니다.

## 에이전트 응답 지침

설명과 작업 결과는 한국어로 작성하세요. 코드, 명령어, 파일명, API 이름처럼 영어가 필요한 경우를 제외하고는 간결하고 이해하기 쉬운 한국어 표현을 사용합니다. 모든 답변은 반말이 아닌 존댓말로 작성하세요.

  - 기본 답변은 결론부터 간결하게 작성하세요.
  - 사용자가 요청하지 않은 배경 설명, 향후 단계, 상태값, 기획안, 아이템 목록은 나열하지 마세요.
  - 단순 질문에는 3~6문장 이내로 답변하세요.
  - 상세 설명이 필요할 수 있으면 먼저 핵심만 답하고, 사용자가 추가로 질문할 때 확장하세요.
  - 선택지가 필요하면 가장 권장하는 방법 하나를 먼저 제시하세요.
  - 작업 결과 보고는 변경 사항과 검증 결과만 간단히 전달하세요.
  - 사용자가 “자세히”, “이유”, “전체 과정” 등을 요청한 경우에만 상세히 설명하세요.
